import styled from "styled-components";

export const TooltipStandardContentBox = styled.div`
    display: flex;
    flex-direction: column;
    gap: 4px;
    padding: var(--ion-space-16);
    max-width: 350px;
    font-size: var(--ion-font-size-body2);
    line-height: 1.3;
`
