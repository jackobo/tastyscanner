import styled from "styled-components";
import {IonChip} from "@ionic/react";

export const FilterValueBox = styled(IonChip)`
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    text-align: center;
    min-width: 70px;
    
`
