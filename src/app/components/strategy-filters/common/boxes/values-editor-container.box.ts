import styled from "styled-components";

export const ValuesEditorContainerBox = styled.div`
    position: relative;
    display: flex;
    flex-direction: row;
    align-items: center;
    gap: var(--ion-space-8);
    width: 200px;
`
