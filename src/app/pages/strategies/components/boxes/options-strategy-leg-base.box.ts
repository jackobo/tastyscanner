import styled from "styled-components";

export const OptionsStrategyLegBaseBox = styled.div`
    display: grid;
    grid-template-columns: 25px 12px 45px 40px 45px 45px;
    align-items: center;
    justify-content: center;
    gap: 8px;
    border-radius: var(--ion-border-radius);
    text-align: center;
`